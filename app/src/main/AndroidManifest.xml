<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ece1778.perls">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AudioActivity"></activity>
        <activity android:name=".ActionActivity" />
        <activity android:name=".SelectorActivity" />
        <activity android:name=".LessonActivity" />
        <activity android:name=".ReflectionReview" />
        <activity android:name=".ReflectionActivity" />
        <activity android:name=".ExerciseActivity" />
        <activity
            android:name=".EmotionSelector"
            android:launchMode="singleInstance" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".AgitationDetectorService" />
    </application>

</manifest>